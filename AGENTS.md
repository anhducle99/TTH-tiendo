# AGENTS.md

## Mục tiêu dự án

Xây dựng ứng dụng **Tiến độ dự án PTPK** theo hướng đơn giản, dễ sử dụng và dễ bảo trì. Dự án đang ở giai đoạn làm rõ yêu cầu; không tự giả định ý nghĩa nghiệp vụ của PTPK hoặc chọn kiến trúc lớn khi chưa có đủ dữ liệu.

## Cách làm việc

- Trao đổi với người dùng chủ yếu bằng tiếng Việt; giữ nguyên thuật ngữ kỹ thuật tiếng Anh khi rõ nghĩa hơn.
- Trước khi thay đổi, đọc `README.md`, `docs/PROJECT-BRIEF.md`, `docs/DECISIONS.md` và kiểm tra trạng thái repository.
- Phân biệt rõ Business Problem, User Need, Business Rule và Technical Solution.
- Chỉ hỏi lại khi thiếu quyết định có thể làm thay đổi đáng kể phạm vi, dữ liệu hoặc kiến trúc.
- Với quyết định nhỏ, chọn phương án đơn giản, phổ biến và có thể đảo ngược; nêu rõ giả định.
- Không thêm dependency, service trả phí hoặc hạ tầng mới nếu chưa chứng minh được sự cần thiết.
- Không ghi secret, token, mật khẩu hoặc dữ liệu nhạy cảm vào source code và tài liệu Git.
- Không xóa dữ liệu, thay đổi cấu hình máy hoặc publish ra ngoài khi chưa được yêu cầu rõ ràng.

## Nguyên tắc đánh giá kép (Senior Code Architect & BA Review)

Trước khi thực hiện bất kỳ thay đổi nào hoặc bắt tay vào code chức năng mới, **BẮT BUỘC** phải phân tích và đánh giá dưới 2 lăng kính:

1. **Góc nhìn Business Analyst (BA):**
   - Đánh giá sự phù hợp của chức năng với mục tiêu bài toán và giá trị kinh doanh thực tế.
   - Nhận diện rủi ro nghiệp vụ, "điểm mù" (blind spots), mâu thuẫn giữa các quy trình (ví dụ: luồng duyệt, ma trận phân quyền, phạm vi dữ liệu).
   - Đề xuất câu hỏi làm rõ các kịch bản biên (edge cases) và phân kỳ giải pháp (phased rollout) tránh over-engineering.

2. **Góc nhìn Chuyên gia Kiến trúc Code (Senior Code Architect):**
   - Đánh giá tính khả thi kỹ thuật, tác động schema/data migration, RLS & Security.
   - Tính nhất quán với mô hình hiện tại (modular monolith, clean types, performance).
   - Dự báo blast radius (ảnh hưởng diện rộng) và đề xuất phương án kỹ thuật tối ưu, dễ bảo trì, dễ mở rộng và dễ đảo ngược.

Chỉ khi hai góc nhìn trên đã thống nhất giải pháp phù hợp và được người dùng xác nhận mới tiến hành viết code/chạy thay đổi.

## Khi phân tích nghiệp vụ

- Làm rõ stakeholder, current state, desired state, phạm vi, business rule, ngoại lệ và edge case.
- Khi phù hợp, chuyển yêu cầu thành User Story và Acceptance Criteria kiểm thử được.
- Cập nhật kết quả đã chốt vào `docs/PROJECT-BRIEF.md`; ghi quyết định quan trọng vào `docs/DECISIONS.md`.

## Khi viết code

- **Phong cách viết code tự nhiên như lập trình viên con người:**
  - Viết code gãy gọn, tự nhiên, đặt tên biến/hàm rõ nghĩa theo đúng convention của dự án (`camelCase`, `PascalCase`).
  - **TUYỆT ĐỐI KHÔNG** chèn comment mang phong cách máy móc của AI (ví dụ: `// Generated by AI`, `// AI assistant comment`, `// Added by assistant`, các khối comment dài dòng thừa thãi giải thích những điều hiển nhiên).
  - Chỉ viết comment khi thật sự cần thiết để giải thích lý do nghiệp vụ phức tạp (business logic) hoặc cảnh báo kỹ thuật đặc thù, theo đúng phong cách của một senior developer.
  - Không để lại bất kỳ từ khóa hay dấu vết nào liên quan đến AI trong source code, commit message hay tài liệu nội bộ.
- Tuân theo cấu trúc và convention đã có; ưu tiên thay đổi nhỏ, dễ review.
- Không thay đổi hành vi hiện có ngoài phạm vi yêu cầu.
- Xử lý trạng thái loading, empty, error và quyền truy cập khi có liên quan.
- Có validation ở ranh giới nhập liệu và thông báo lỗi dễ hiểu.
- Sau khi chỉnh sửa, chạy formatter, lint, type-check, test và build tương ứng với stack đang dùng.
- Nếu ứng dụng có giao diện, chạy ứng dụng và kiểm tra luồng chính trên trình duyệt trước khi kết luận hoàn thành.

## Tài liệu hướng dẫn người dùng (User Guide)

- Luôn duy trì và cập nhật tài liệu hướng dẫn sử dụng chi tiết tại `docs/USER-GUIDE.md`.
- **Mỗi khi có thay đổi, bổ sung hoặc tối ưu chức năng/nghiệp vụ:** Bắt buộc phải cập nhật ngay vào `docs/USER-GUIDE.md`.
- Nội dung hướng dẫn phải trực quan, dễ hiểu với người dùng cuối, minh họa luồng thao tác từng bước kèm hình ảnh chụp thực tế hoặc sơ đồ minh họa quy trình.

## Definition of Done

Một hạng mục chỉ hoàn thành khi:

1. Đáp ứng Acceptance Criteria đã thống nhất.
2. Không làm hỏng hành vi liên quan.
3. Các kiểm tra tự động liên quan đều đạt.
4. Luồng giao diện chính đã được xác minh nếu có UI.
5. Cập nhật tài liệu kỹ thuật, quyết định liên quan (`docs/DECISIONS.md`, `docs/PROJECT-BRIEF.md`) VÀ **cập nhật tài liệu hướng dẫn người dùng (`docs/USER-GUIDE.md`) kèm hình ảnh minh họa**.
6. Tóm tắt rõ file đã đổi, cách kiểm tra và rủi ro còn lại.


